<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lcdzim">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:configChanges="orientation|keyboardHidden"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".CreateEditCasePlanActivity"></activity>
        <activity
            android:name=".CreateEditCaseLogActivity"
            android:theme="@style/NoTitleDialog" />
        <activity
            android:name=".CreateEditCaseWorkPlanActivity"
            android:theme="@style/NoTitleDialog" />
        <activity android:name=".CreateEditPaymentsToBeneficiaryActivity" />
        <activity android:name=".ListPaymentsToBeneficiariesActivity" />
        <activity android:name=".CreateEditJustificationReportActivity" />
        <activity android:name=".ListJustificationReportsActivity" />
        <activity android:name=".CreateEditCaseReportActivity" />
        <activity
            android:name=".ListCasesActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>